const indexPrizes = require('./indexPrizes')
const indexScores = require('./indexScores')
const scoreLineups = require('./scoreLineups')
const rankLineups = require('./rankLineups')
const attributePrizes = require('./attributePrizes')
const saveAttributtedLineups = require('./saveAttributtedLineups')

module.exports = {
  indexPrizes,
  indexScores,
  scoreLineups,
  rankLineups,
  attributePrizes,
  saveAttributtedLineups
}
